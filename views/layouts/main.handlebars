<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    {{#if styles}}
    {{#each styles}}
    <link rel="stylesheet" href="/css/{{this}}">
    {{/each}}
    {{/if}}
    {{#if scripts}}
    {{#each scripts}}
    <script defer src="/js/{{this}}"></script>
    {{/each}}
    {{/if}}
    <title>{{title}} - JTools</title>
</head>

<body>
    {{!-- NAVBAR --}}

    {{!-- MAIN CONTENT --}}
    {{{body}}}
    {{!-- FOOTER --}}
</body>

</html>